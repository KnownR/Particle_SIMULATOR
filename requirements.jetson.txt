# Jetson Nano runtime Python deps.
#
# OpenCV:          provided by APT (python3-opencv), NOT pip.
# jetson_utils:    provided by the dustynv/jetson-inference container.
# jetson_inference: provided by the dustynv/jetson-inference container.
#
# These pip deps are installed in the Dockerfile.jetson build step.
# You should NOT need to run this file manually if using the Docker workflow.

numpy>=1.19,<2.0
